# نقشه راه و پلن جامع توسعه و بازطراحی اکستنشن تقویم شمسی گنوم (GNOME 46 - 50)
## Iranian Persian Calendar (Modern Edition)

این سند معماری فنی، استانداردها و بهترین روش‌های توسعه (Best Practices) برای بازطراحی، بهینه‌سازی، مدرن‌سازی و اضافه کردن قابلیت‌های همگام‌سازی تقویم گوگل به افزونه را تعیین می‌کند.

---

### ۱. اصول و استانداردهای طراحی بر اساس GNOME 46+ و Libadwaita

1. **معماری ماژولار و ESM:**
   - استفاده دقیق از استاندارد ES Modules در GJS مطابق مستندات رسمی GNOME 45+.
   - وارد کردن ماژول‌های شل از `resource:///org/gnome/shell/...` و کتابخانه‌های GI از `gi://...`.
   
2. **مدیریت بدون نشت منابع و سیگنال‌ها (Clean Signal Registry & Memory Safety):**
   - پیاده‌سازی کلاس یا آرایه مدیریت متمرکز سیگنال‌ها (`SignalTracker`) به طوری که تمام `connect`ها با نام شناسه ثبت شده و در متد `disable()` به صورت قطعی دیسکانکت و تخریب شوند.
   - پاک‌سازی تمام تایمرهای `GLib.timeout_add_seconds` و رفرنس‌های آبجکت‌ها برای جلوگیری از `Memory Leak`.

3. **حذف دستکاری دیسک و پیروی از سکیوریتی شل:**
   - حذف کامل کدهای کپی و حذف فونت در مسیر خانه کاربر (`~/fonts`).
   - اتکا به فونت‌های مدرن سیستم (`Vazirmatn`, `Sahel`, `IRANSans` یا `Cantarell`) از طریق تعاریف بهینه CSS.

4. **طراحی واکنش‌گرا و سازگار با تم تیره/روشن سیستم (Dynamic Theming & Libadwaita Tokens):**
   - جایگزینی رنگ‌های هاردکد شده با متغیرهای رنگی استاندارد گنوم شل (`theme_selected_bg_color`، `theme_fg_color`، `card_bg_color`).
   - استفاده از کامپوننت‌های مدرن `Adw.PreferencesPage`، `Adw.PreferencesGroup`، `Adw.SwitchRow`، `Adw.ComboRow` و `Adw.ActionRow` در پنجره تنظیمات (`prefs.js`).

5. **اتصال به تقویم گوگل از طریق زیرساخت بومی گنوم (Evolution Data Server / EDS):**
   - استفاده از سرویس بومی `ECal` و `EDataserver` در گنوم بدون نیاز به لاگین یا توکن مجزا.
   - خواندن مستقیم رویدادهای حساب گوگل که در `GNOME Online Accounts (GOA)` فعال شده‌اند.
   - تبدیل محدوده زمانی رویدادهای میلادی گوگل به روزهای معادل شمسی جهت نمایش نشانگر روی روزهای تقویم و لیست رویدادها.

6. **بهینه‌سازی کارایی محاسبات تقویم و کَشینگ (Event In-Memory Cache):**
   - ذخیره و کَش داده‌های مناسبت‌های رسمی و رویدادهای کاربر برای ماه انتخابی در قالب `Map<JulianDay, Event[]>` جهت دستیابی به زمان پاسخ $O(1)$ در هر کلیک و تغییر تاریخ.

---

### ۲. فازبندی اجرایی پروژه (Implementation Phases)

#### 🔹 فاز ۱: پاک‌سازی، حذف بخش‌های مذهبی/اذان و اصلاح چرخه حیات (De-bloat & Life-Cycle Fixes)
- [ ] حذف فایل‌های `PrayTimes.js`، `cities.js`، `sound.js` و پوشه `sounds/`.
- [ ] حذف کلیدهای مربوط به اذان، شهر و پخش صدا از `schemas/org.gnome.shell.extensions.shamsi-calendar.gschema.xml`.
- [ ] پاک‌سازی متدهای `install_fonts` و `uninstall_fonts` از `extension.js`.
- [ ] حذف تایمرهای لوپ هر دقیقه‌ای اذان و بازنویسی چرخه تایمر تغییر روز.
- [ ] کامپایل مجدد اسکیما (`glib-compile-schemas`) و اعتبارسنجی اجرای بدون خطای افزونه پایه.

#### 🔹 فاز ۲: مدرن‌سازی ظاهر و رابط کاربری (UI/UX Redesign)
- [ ] بازنویسی کامل `stylesheet.css` با استایل‌های مینیمال، گوشه‌های گرد، پدینگ‌های مدرن و پشتیبانی ۱۰۰٪ از حالت تیره/روشن.
- [ ] بازطراحی هدر تقویم و نوار دکمه‌های ناوبری ماه/سال با آیکون‌های استاندارد Adwaita.
- [ ] اصلاح گرید روزها در `calendar.js` و نمایش نشانگرهای رویداد و روز جاری به سبک شل مدرن.
- [ ] بازطراحی برچسب و دکمه پنل بالا (`Panel Indicator`) با امکان سفارشی‌سازی نمایش تاریخ و فرمت مینیمال.

#### 🔹 فاز ۳: سیستم رویدادها و اتصال به Google Calendar (EDS Integration)
- [ ] بازنویسی ماژول `Events.js` با قابلیت کَش کردن مناسبت‌های رسمی کشور.
- [ ] پیاده‌سازی سرویس خواندن تقویم گوگل از طریق `GNOME Evolution Data Server (libecal/EDataserver)`.
- [ ] ادغام رویدادهای تقویم گوگل با مناسبت‌های رسمی و نمایش هم‌زمان در سایدبار/تب رویدادهای تقویم.
- [ ] تبدیل خودکار تاریخ و ساعت رویدادهای گوگل به تقویم شمسی.

#### 🔹 فاز ۴: بازنویسی پنجره تنظیمات با Libadwaita (Modern Preferences)
- [ ] بازنویسی `prefs.js` با چیدمان تمیز `Adw.PreferencesPage` و گروه‌بندی منظم:
  - **نمایش در پنل:** فرمت تاریخ، موقعیت در پنل، نمایش روزهای تعطیل.
  - **ظاهر و تم تقویم:** اندازه، نوع فونت، هایلایت روزها.
  - **همگام‌سازی تقویم:** کلید فعال‌سازی Google Calendar و انتخاب تقویم‌های فعال.
  - **مناسبت‌های رسمی:** فعال/غیرفعال کردن دسته‌های مختلف رویدادهای ملی و جهانی.
- [ ] تست کامل پنجره تنظیمات و اعتبارسنجی تغییرات در لحظه (`Live Settings Binding`).

#### 🔹 فاز ۵: تست، کنترل کیفیت و آماده‌سازی برای استفاده
- [ ] تست اجرای اکستنشن در GNOME 50.
- [ ] تست عملکرد در زمان تغییر روز و تغییر منطقه زمانی (`Timezone Change`).
- [ ] بررسی نهایی عدم وجود هرگونه اخطار یا خطای لاگ در `journalctl -f / gjs`.
- [ ] تهیه مستندات نصب محلی و ساخت پکیج نهایی (`gnome-extensions pack`).

---
